<div class="left-block">
    <h5>{{"Top users" | trans}}</h5>
    <ul>
    {% for user in users %}
        <li><a class="liensGray" href="{{ path('user_details', { 'id': user.user_id }) }}">{{user.username}}</a></li>
        
    {% endfor %} 
        <li><a class="liensRouge" id="top-user-list" href="{{ path('top_users') }}">{{ "view all list"|trans }}</a></li>
    </ul>    
</div>